# PrintableCheckList — SwiftUI rewrite

This is a clean SwiftUI rewrite of the Objective-C app in
`../PrintableCheckList`. It keeps the original product name (`Flash`), bundle
identifier, information architecture, list editing behavior, printable preview,
settings, localizations, and default travel checklist. The presentation uses
current native SwiftUI/iOS components instead of copying the 2015 styling.

The project is generated by XcodeGen and can be built, tested, and run without
opening the Xcode GUI.

```bash
./Scripts/generate.sh
./Scripts/build.sh
./Scripts/test.sh
./Scripts/run-simulator.sh
# Signed device/App Store archive (requires an Apple development team)
./Scripts/archive.sh
```

## Migration status

- Local-first JSON persistence: implemented
- Legacy Objective-C `NSKeyedArchiver` migration: implemented
- Lists and items add/edit/delete/reorder: implemented
- Print and preview: implemented
- Settings actions and localization: implemented
- Original iCloud key-value sync: implemented, including old archive import
- Optional LeanCloud-style developer snapshot upload via Supabase: implemented and disabled until configured

## Configure optional developer snapshot uploads

The old app uploaded an installation-scoped JSON copy of the current lists to
LeanCloud. It never downloaded or restored that data; iCloud was and remains
the user-facing device-to-device sync mechanism. The rewrite can optionally
send the same kind of one-way snapshot to a developer-owned Supabase project.
This backend is not shown or configured in the app's Settings UI.

1. Create a Supabase project and enable anonymous sign-ins under Authentication.
2. Run `Supabase/migrations/20260721000000_create_checklist_snapshots.sql` in
   the project's SQL editor.
3. Copy `Config/Local.xcconfig.example` to `Config/Local.xcconfig`.
4. Set `SUPABASE_URL` and the project's **publishable key**. Never embed a
   `service_role` or secret key in the app.
5. Build and run. On launch, the app uploads at most once per hour per
   installation, matching the old LeanCloud throttle.

Anonymous Supabase users preserve the old app's installation-scoped behavior.
iCloud still provides cross-device synchronization. Supabase snapshots are not
a user backup or recovery feature and are never downloaded by the app.

The payload contains user-entered list text. Enable this optional upload only
after making an explicit product and privacy decision, and configure appropriate
abuse controls for anonymous sign-ins. Otherwise leave the Supabase values empty.

### Xcode Cloud

Add `SUPABASE_URL` and `SUPABASE_PUBLISHABLE_KEY` to the workflow's Environment
Variables. Mark the publishable key as secret to keep it redacted from build
logs. Do not configure a `service_role` or `sb_secret_` key.

Xcode Cloud runs `ci_scripts/ci_post_clone.sh` after cloning. The script writes
the ignored `Config/Local.xcconfig`, installs XcodeGen with Homebrew when needed,
and generates `PrintableCheckList.xcodeproj` from `project.yml`. The URL entered
in Xcode Cloud must use its normal `https://` form; the script applies the
escaping required by `.xcconfig` files.

## Configure signing and iCloud

Copy `Config/Local.xcconfig.example` to `Config/Local.xcconfig`, then set
`DEVELOPMENT_TEAM` to the Apple team ID that owns
`com.wehack.PrintableCheckList`. The checked-in entitlement retains the
original key-value-store identifier. A signed device build is required for a
real iCloud end-to-end test; unsigned simulator builds intentionally cannot
access the production iCloud container.

Xcode must be installed because Apple's SDK, compiler, simulator, signing, and
archive tools come from Xcode, but none of these commands require opening the
Xcode GUI.
